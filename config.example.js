/**
 * Configuration example.
 * COPY THIS FILE to config.js and replace REPLACE_WITH_YOUR_API_KEY with your API key.
 *
 * IMPORTANT:
 * - Do NOT commit your real config.js containing the private key to a public repo.
 * - For production, prefer a serverless proxy or backend to hide your key.
 */

const CONFIG = {
  API_BASE: "https://api.example.com/v1", // replace with your actual API base
  API_KEY: "REPLACE_WITH_YOUR_API_KEY",   // <-- put your key here in local config.js
  AFFILIATE_LINK: "https://affiliate.example.com/?campaign=streamly" // your CPA affiliate URL
};